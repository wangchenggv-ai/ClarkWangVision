# 多项目编程上下文管理最佳实践

> 适用场景：多个项目并行开发，每次重启项目时 AI 重建上下文成本过高（单次 1M+ token）
> 核心思路：把"项目记忆"结构化沉淀，让 AI 读文档而不是读代码

---

## 一、核心原则

**用"写入"替代"读取"**

每次结束工作时多花 5 分钟写交接文档，可以省下次启动时 30 分钟的代码重读 + 数十万 token。这和管理团队写周报是同一个逻辑——结构化沉淀比每次重新口述更高效。

**禁止动作**：让 AI 一上来就 `read` 整个项目目录或 `cat` 大文件。

---

## 二、三层文件体系（每个项目都建）

### 第 1 层：`CLAUDE.md`（项目宪法，长期稳定）

放在项目根目录，200-500 行，包含：

- **项目一句话定位**：这个项目是干什么的
- **架构总览**：模块划分、依赖关系、数据流向
- **关键文件清单**：每个核心文件一句话说明作用
- **技术栈 + 版本**：语言、框架、关键库的具体版本
- **编码约定**：命名规范、目录结构、提交规范
- **已知坑点 / 红线**：不要碰的地方、历史教训

**更新频率**：架构变化时更新，平时不动。

### 第 2 层：`STATE.md`（工作交接，每日更新）

每次结束工作前由 AI 自动生成，100-300 字：

```markdown
## 最后更新：YYYY-MM-DD HH:MM

### 今日完成
- [具体做了什么]

### 明日继续
- [下一步要做什么，从哪个文件开始]

### 当前卡点
- [遇到的问题、未决策的事项]

### 关键文件（本次涉及）
- path/to/file1.py：[修改了什么]
- path/to/file2.py：[修改了什么]
```

**最高 ROI 动作**：几百 token 写入，省下次几十万 token 读入。

### 第 3 层：按需加载

AI 启动时只读 `CLAUDE.md` + `STATE.md`（合计 <2K tokens），具体任务相关的文件由 AI 自己 grep / find 查找,不预加载。

---

## 三、AI 启动标准提示词

新开会话时,直接发这段:

```
请按以下顺序加载上下文,不要主动读取其他文件:
1. 读 CLAUDE.md 了解项目架构
2. 读 STATE.md 了解当前进度
3. 看最近 5 条 git log --oneline 了解近期改动
4. 告诉我你理解的当前状态,等我确认后再开始工作
```

`git log` 比读代码快 100 倍,且能给 AI 时间线感知。

---

## 四、6 个项目的总览管理

### 建立 `~/projects/INDEX.md`

```markdown
# 项目总览

| 项目 | 状态 | 优先级 | 阶段 | 最后活跃 |
|------|------|--------|------|----------|
| 项目A | 🟢活跃 | P0 | 开发中 | 今天 |
| 项目B | 🟡缓慢 | P1 | 联调 | 3天前 |
| 项目C | 🔵冻结 | P2 | 完成v1 | 2周前 |
```

避免在 6 个项目间切换时上下文混淆。

### 项目状态管理

- **活跃项目**:固定使用同一个会话,减少冷启动
- **不活跃项目**:写 `HIBERNATION.md` 冻结状态,复活时只读这一个文件即可恢复
- **完成项目**:归档,从 INDEX 中移除

---

## 五、Token 节省效果预估

| 做法 | 单次启动 token 消耗 |
|------|---------------------|
| 让 AI 自己读代码理解 | 500K - 1M+ |
| 只读 CLAUDE.md + STATE.md | 2K - 5K |
| **节省比例** | **>99%** |

按你目前 6 个项目、每天 2 次重建上下文计算,预计每天可节省 **数 M token + 1-2 小时时间**。

---

## 六、落地步骤(建议本周完成)

1. **今天**:为最活跃的 1 个项目建 `CLAUDE.md` + `STATE.md`,验证效果
2. **本周**:推广到全部 6 个项目
3. **建立习惯**:每次结束工作前,让 AI 自动更新 `STATE.md`(可加到提示词模板)
4. **建 INDEX.md**:总览 6 个项目状态

---

## 七、配套工具建议

- **Cursor / Claude Code**:原生支持 `CLAUDE.md` / `.cursorrules`,会自动加载
- **Git hooks**:可设置 pre-commit hook 自动提醒更新 STATE.md
- **模板复用**:把 CLAUDE.md / STATE.md 模板存到 Obsidian vault,新项目直接套用

---

## 八、与你现有工作流的衔接

- **Obsidian vault**:可在 `ClarkWangVision` 中建 `编程项目总览` 笔记,链接到各项目的 INDEX.md
- **AI 使用纪律**:符合你"先写假设再执行"的原则——CLAUDE.md 就是项目级假设的固化
- **Token 止损**:配合你 2M token 止损规则,如果发现 AI 还在大量读代码,说明 CLAUDE.md 写得不够好,需要补充

---

人是主驾,AI 是副驾@Clark
